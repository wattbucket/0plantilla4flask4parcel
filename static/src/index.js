import './index.scss';

let container = document.getElementById('root');
let element = document.createElement('h1');
let text = document.createTextNode('adessso si ,va ddddbenissssssiomo');

element.appendChild(text);
container.appendChild(element);
